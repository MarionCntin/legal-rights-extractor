[project]
name = "lre"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.0",
  "python-dotenv>=1.0",
  "pypdf>=4.0",
  "python-docx>=1.0",
  "typer>=0.12",
  "numpy",
  "rank-bm25"
]

[project.scripts]
lre = "lre.cli:app"
