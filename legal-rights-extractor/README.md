# legal-rights-extractor (lre)

Local-first, auditable extraction pipeline for legal rights.

## Setup

```bash
cd legal-rights-extractor
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .
